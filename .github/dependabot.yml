# Keeps dependencies and workflow pins from rotting across all package ecosystems in the repo.
#
# Ecosystems covered:
# - npm (root workspace / frontend)
# - npm (server workspace / backend)
# - docker (Dockerfile base images)
# - github-actions (.github/workflows actions)
#
# Grouped into single PRs per ecosystem to keep update volume manageable.
version: 2
updates:
  - package-ecosystem: npm
    directory: /
    schedule:
      interval: weekly
      day: monday
    open-pull-requests-limit: 5
    ignore:
      - dependency-name: typescript
    groups:
      npm-dependencies:
        patterns:
          - '*'
    commit-message:
      prefix: 'chore(deps)'

  - package-ecosystem: npm
    directory: /server
    schedule:
      interval: weekly
      day: monday
    open-pull-requests-limit: 5
    groups:
      server-dependencies:
        patterns:
          - '*'
    commit-message:
      prefix: 'chore(deps)'

  - package-ecosystem: docker
    directory: /
    schedule:
      interval: weekly
      day: monday
    open-pull-requests-limit: 5
    groups:
      docker-dependencies:
        patterns:
          - '*'
    commit-message:
      prefix: 'chore(docker)'

  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
    open-pull-requests-limit: 5
    groups:
      actions:
        patterns:
          - '*'
    commit-message:
      prefix: 'chore(actions)'
