# Same rule as the root .npmrc: npm only reads the .npmrc of the directory it
# runs in, and this workspace is installed with `npm ci` from inside server/.
engine-strict=true

# Same reason as the root .npmrc: the nested postinstall `npm install` in
# this directory only reads this file, so the audit skip has to be repeated.
audit=false
