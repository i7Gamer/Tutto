# ─── Security Token ────────────────────────────────────────────────────────────
# Secret token shared between the frontend (Vite) and backend (Express).
# Used to authenticate API requests like posting game statistics.
# Set a strong random value in production (e.g. openssl rand -hex 32).
VITE_API_TOKEN=tutto-local-dev-token

# ─── Backend (Node / Express) ────────────────────────────────────────────────
# Port the Express server listens on (default: 3001).
PORT=3001

# Allowed CORS origin for both the HTTP API and the Socket.IO server. Defaults
# to '*' (any origin) when unset, which is fine for local dev/LAN play. Set
# this to the deployed origin in production, e.g.:
# CORS_ORIGIN=https://tutto.rzipas.win
CORS_ORIGIN=*

# NOTE: NODE_ENV is NOT set here — this file is loaded by Vite, which sets
# NODE_ENV itself based on the build mode. For the Node server, NODE_ENV must
# be set in the shell. Use `npm run start:prod` which sets it automatically.
